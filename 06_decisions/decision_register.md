# PROJECT-01 Decision Register — INITIAL

Per the portfolio workflow governance report (P01-07). Consolidates every open decision surfaced across P01-01..06 (`03_specification/scenario_matrix.md`, `04_interface/i2c_interface_contract.md`, `05_power/power_state_model.md`, `05_power/power_budget.md`). Per `docs/00_shared/engineering_rules.md` #8, major decisions and their rationale are recorded here.

## Evidence-acquisition constraint

Targeted verification for GAP-01..06 was first attempted via `WebSearch`; direct primary-source retrieval (`WebFetch`) remains blocked in this session's network policy for every external domain tested (Espressif, TI, Bosch Sensortec, Mouser, and a neutral control domain). That constraint is unchanged and still applies to anything not explicitly closed below.

**Update (2026-09-17, batch 1):** the user directly supplied primary-manufacturer datasheet values (ESP32 Series Datasheet v5.3; TI TPS7A02 datasheet; Bosch BME280 datasheet BST-DS002; Microchip MCP73871 datasheet DS22090), each attributed to a named primary source. These are recorded below as `SOURCE_SUPPORTED` (page/table numbers were not given, but the values are attributed to specific primary documents by name, not to a secondhand paraphrase) — a materially stronger evidence tier than the earlier `WebSearch`-snippet-corroborated candidates, which are now superseded wherever a primary value was supplied.

**Update (2026-09-17, batch 2):** the user then authorized targeted `WebSearch` research specifically for GAP-02, GAP-03 and GAP-08, and new-component discovery for DEC-11 (both previously withheld). Findings from this batch are `WebSearch`-snippet-corroborated, not primary-source-read — a weaker tier than batch 1, and explicitly not promoted to `SOURCE_SUPPORTED` on that basis alone. Where a finding is a standard's well-established public constant (e.g., I2C UM10204's own VOL/IOL/rise-time limits) rather than a proprietary datasheet figure, that distinction is noted per item.

**Update (2026-09-17, batch 3 — under the 17.09.2026 REFERENCE_BASELINE governance):** the user pointed to an external, independently-run research session (relayed via Drive screenshots, "Araştırma Kayıtları"/"Referans Listeler", 15.09.2026) that had genuine primary-source access and cited Microchip/TI/Espressif/Bosch/NXP documents directly, by parameter name and test condition. Treated as `SOURCE_SUPPORTED (relayed)`: stronger than a `WebSearch` snippet (specific parameter/condition, not a paraphrase) but weaker than batch 1 (conveyed through a third party's transcript, not opened by this session or typed by the user directly). This batch closes GAP-08 and GAP-02 (`evidence_gaps.md`) and **reopens DEC-01, DEC-02 and DEC-04 below** — it surfaced that MCP73871 has only one physical power input (not two independent USB+solar pins) and that TPS7A02's 200 mA output ceiling is below the ESP32-WROOM-32E's own recommended supply capability.

**Update (2026-09-18, batch 4 — external handoff document, `claude-handoff-gate2.pdf`):** the user supplied a structured handoff document (independently authored, consistent with this repo's own GATE-2 state) naming candidate components for DEC-01/DEC-02/DEC-04 (TPS2113A automatic power mux; LTC4412 + external P-channel MOSFET ideal-diode-OR controller, "approximately 11 µA quiescent current is documented"; BQ24074 as a charger alternative) and a full proposed Rev-A block architecture. Component figures from this document are `SOURCE_SUPPORTED (handoff-relayed)` — same tier as batch 3: attributed to a named part and, for LTC4412, a specific Iq figure described as "documented," but not opened by this session against the primary Linear Technology/Analog Devices datasheet. **The user then explicitly authorized proceeding on DEC-01/DEC-02/DEC-04/DEC-13 using stated `ASSUMED` values for solar-panel and battery parameters** (not yet a real selected panel/cell), so that topology decisions could be closed without waiting on those inputs. Those assumptions are recorded in a new "Design-input assumptions" section below and must be revisited once real panel/cell selections exist. No PCB/firmware implementation was performed in response to any of the four updates; the user has not authorized a merge of PR #1.

**Update (2026-09-18, batch 5 — revised handoff, `Codex_Handoff_CAN_Bus_Enerji_Uretimi_Portfoy_REVISED_CLOSURE_18.09.2026.docx`):** this revision changes the *operating model*, not just the evidence: broad product research and component selection are now delegated to a separate research project; this repository ("Codex" in the handoff's own terms) implements only FROZEN, evidence-backed closure packages returned by that process — it does not perform open-ended product discovery itself. The revision also explicitly restates that seed candidates ("worth comparing") are **not** selected components until a research-plus-calculation closure package accepts one (its §4, §15, §19: "no seed candidate is silently promoted to selected component"). Under that rule, three of batch 4's outcomes are reconciled below:

- **DEC-01/DEC-02 (source arbitration) downgraded.** Batch 4 recorded "DECIDED (candidate) — LTC4412 + external P-channel MOSFET" on handoff-relayed evidence alone (no 2–3-candidate comparison, no reproducible calculation, no bench validation plan beyond a to-do note). That was a premature promotion under this revision's own rule. **Reverted to `RESEARCH_REQUIRED`**, tracked as **P01-R01** below; LTC4412+P-MOSFET and TPS2113A-class both remain seed candidates only.
- **DEC-13 (battery-side protection) downgraded.** Batch 4 recorded "DECIDED (candidate) — mandate protected 1S cell." The revision's own P01-R04 closure target ("freeze protected-1S-cell boundary or external protection architecture; include cutoff/leakage/failure assumptions") is not met — no specific cell, cutoff or leakage evidence exists. **Reverted to `RESEARCH_REQUIRED`**, tracked as **P01-R04**.
- **DEC-04 (3.3 V regulator) unchanged.** Its "topology only, part OPEN" status already matches this revision's model (an architecture-level statement with no part selected) — tracked as **P01-R03**. Two new seed candidates (BQ25185, TPS62840) are added per the revision's §19; see `reference_classification.md` REF-20/21.
- **DEC-12/DEC-11 unchanged.** Both were already OPEN/BLOCKED, consistent with this revision's P01-R05/P01-R06.

**P01-R0x package cross-reference.** The handoff reuses labels like "DEC-01".."DEC-04" with *different meanings* than this register's own DEC numbering (e.g. the handoff's own "DEC-02" means charger/power-path, which is this register's **DEC-03**, not this register's DEC-02). This table maps the handoff's package IDs to this register's actual DEC-IDs to avoid confusion — **this register's own DEC numbering is not renumbered**. The revision's own research dependency order (§21, "revised immediate next action") is **P01-R01 → P01-R02 → P01-R03 → P01-R04 → P01-R05/P01-R06 → P01-R07 → P01-R08**, matching the row order below:

| Handoff package | Handoff's own label | This register's ID(s) | Status after batch 7 |
|---|---|---|---|
| P01-R01 | USB + solar source arbitration | DEC-01, DEC-02 | `DECIDED (candidate)` — TPS2121 (batch 7); Iq unquantified, PV-03 pending |
| P01-R02 | Charger / power-path (handoff calls this "DEC-02") | DEC-03 | **`BLOCKED`** (reopened batch 7) — architecture conflict, MCP73871 abs-max VIN=7.0V < panel V_OC≈7.2–8.0V; two paths flagged, none selected |
| P01-R03 | Main 3.3 V regulator (handoff calls this "DEC-04", matches this register's DEC-04) | DEC-04 | `DECIDED (topology)`; part `PROVISIONAL` (RT9080, batch 7) — fallback TPS63802 if PV-01 fails |
| P01-R04 | Battery protection (handoff calls this "DEC-13", matches this register's DEC-13) | DEC-13 | `PROVISIONAL` (factory-protected 18650, e.g. 30Q-class, batch 7) — procurement + PV-06 pending |
| P01-R05 | BME280 + I2C power gating (handoff calls this "DEC-12", matches this register's DEC-12) | DEC-12 | `DECIDED (candidate)` — TPS22916 (batch 7); PV-04 pending |
| P01-R06 | Always-on leakage (handoff calls this "DEC-11", matches this register's DEC-11) | DEC-11 | `DECIDED (candidate)` — SMAJ9.0A + SMF5.0A/TPD1S514 (batch 7); PV-05 pending |
| P01-R07 | Battery sensing / ADC | DEC-05 (partial) | New package; see `02_requirements/use_case_scenarios.md`, `05_power/calculation_packages.md` P01-C08 |
| P01-R08 | Board-level energy model | SOL-015/SOL-016 (partial) | New package; see `05_power/calculation_packages.md` P01-C01..C04 |

No component selection, PCB implementation, or new product research is performed by this session in response to this revision — per its own operating principle, that belongs to the separate research project. This session's batch-5 action is limited to: (a) reconciling already-recorded decisions to the revision's stricter seed-candidate rule, (b) adding the use-case/calculation package scaffolding it requires (`02_requirements/use_case_scenarios.md`, `05_power/calculation_packages.md`), and (c) recording newly-named seed candidates without selecting any of them.

**Update (2026-09-18, batch 6 — external research handoff, `perplexity-tamamlayici-arastirma-handoff-p01-r01-r06.pdf`):** the separate research project (Perplexity-based) returned its first closure package for P01-R01..R06. Per its own explicit statement (§6, "Claude'un işlem sırası"), this package closes **numeric design-input envelopes only** — it selects no component model number and performs no physical validation. Concretely:

- **Solar panel target envelope** — replaces the batch-4 `ASSUMED` placeholder (V_OC=6.0V, V_MP=5.0V, I_MP=150mA, P_max≈0.75W) with a `SOURCE_SUPPORTED (research-relayed)` 6 V-class/2 W-nominal target: V_MP≈6.0V, V_OC≈7.2V, I_MP≥0.33A, I_SC≈0.35A, P_max≥2W. Attributed to a sample 6V/2W panel datasheet class (STC: 1000 W/m², AM1.5, 25°C), **not a purchased panel** — five physical-validation items (PV-01..05: label/datasheet values, outdoor open-circuit V_OC, real load V/I, USB+solar reverse-current, low-light/shade brownout behavior) remain `PHYSICAL_VALIDATION_REQUIRED`.
- **Battery target envelope** — replaces the batch-4 `ASSUMED` placeholder (1000 mAh) with a `SOURCE_SUPPORTED (research-relayed)` target: protected 1S Li-ion 18650, ≥3000 mAh nominal (2000–3500 mAh research envelope), nominal energy 10.8 Wh at 3000 mAh, sample-datasheet discharge-end reference ≈2.75 V/cell. **Not a purchased cell/protection-PCB model** — cutoff/thermal/current validation remains `PHYSICAL_VALIDATION_REQUIRED`.
- **Daily-energy/autonomy example** — a `CALCULATED` illustration built on explicit `ASSUMED` duty-cycle inputs (96 telemetry events/day, 15 s active/event, 180 mA active current): ≈3.04 mA average current, ≈72.9 mAh/day, ≈32.9-day theoretical autonomy at 80% usable capacity of a 3000 mAh cell. See `05_power/energy_autonomy_calc.py` (new, this batch). **Arithmetic correction (not the source document's own figure):** the returned package states these results as ≈3.32 mA / ≈79.7 mAh/day / ≈30 days — but its own formula, `Iavg = [(180 mA × 1440 s) + (0.040125 mA × 84960 s)] / 86400 s`, evaluates to **3.0395 mA**, not 3.32 mA, when computed exactly (verified by running `energy_autonomy_calc.py`, which implements this same formula). The figures recorded in this register and used everywhere else in this batch are the corrected, reproducibly-verified ones (3.04 mA / 72.9 mAh/day / 32.9 days), not the source document's stated totals. This is exactly the failure mode `docs/00_shared/engineering_rules.md` #4 exists to catch — a manually-typed number in an otherwise-sound external document was not re-derived from its own stated formula before being handed off.
- **DEC-12/BME280 direction** — the package states the existing power-gating topology (VDD+VDDIO switched together on `SENSOR_3V3`, pull-ups on the switched rail, I2C GPIOs forced safe/high-Z before power-down) is `SUPPORTED`, without changing DEC-12's own OPEN status — off-state I2C leakage measurement remains `PHYSICAL_VALIDATION_REQUIRED`.

**Every P01-Rxx component/part-selection question remains `RESEARCH_REQUIRED`** — this package explicitly does not select or promote any part to `DECIDED`: P01-R01 (arbitration part), P01-R02's charge-current tuning against the new panel class, P01-R03 (regulator part), P01-R04 (specific cell/protection-PCB model), and P01-R06 (OVP/TVS part) are all still open; P01-R05's *direction* is confirmed, not closed.

**Correctness flag (not glossed over):** the returned package's daily-energy calculation reuses the batch-3 `40.125 µA typ` sleep-current figure as a working input. That figure was retracted twice in this register — batch 4 (DEC-04 dropped TPS7A02) and structurally reaffirmed in batch 5 — and `power_budget_calc.py`'s `SleepCurrentBudget.total_ua()` currently raises `ValueError` rather than returning a number, because `i_regulator_iq_ua` is `OPEN`. The daily-energy/autonomy example is therefore recorded as an explicitly provisional **what-if** calculation tied to a stale sleep-current input, not a live figure from this project's own budget — `05_power/energy_autonomy_calc.py` carries this warning in its own docstring and must be re-run with the real total once P01-R03's part is selected and `power_budget_calc.py` actually computes.

**Update (2026-09-19, batch 7 — external research handoff, `Solar-Powered_ESP32_Sensor__GATE-2_Component_Selection_Research_P01-R01_to_R06.pdf`):** the separate research project returned its second closure package, this time proposing actual candidate *components* (not just numeric envelopes) for P01-R01..R06, each with primary-datasheet citations, an explicit 2–3-candidate comparison, rejected alternatives, and named residual physical-validation items (PV-01..PV-07). Per its own explicit statement, "hiçbir karar DECIDED değil" (no decision is DECIDED) — every row is a "recommended candidate." This batch's evidence bar is materially stronger than batch 4's (which this register's own batch-5 pass correctly rejected as premature): here, comparisons, calculations and rejected alternatives all exist, matching the closure requirements DEC-01/DEC-11 themselves already specified. Applied per-package below:

- **P01-R01 (DEC-01/DEC-02) promoted to `DECIDED (candidate)`** — TI TPS2121 (priority-mux ideal-diode-OR, 2.8–22V input per its own datasheet SLVSDR8), replacing the earlier LTC4412/TPS2113A-class candidates entirely: LM66100/LM66200/TPS2113A are all rejected on the same basis (5.5–6V abs-max, cannot tolerate the batch-6 panel's ~7.2–8.0V raw Voc), and Schottky OR-ing (SS34) is kept only as a simpler fallback (accepts a ~0.4–0.5V charger-input-margin penalty). **Not fully closed:** TPS2121's own sleep-current Iq is not quantified anywhere in the returned package (only qualitatively "düşük-Iq" / low-Iq) — the `I_arbitration_Iq` sleep-budget term remains `ASSUMED`/`OPEN`, now attributed to an unquantified TPS2121 rather than the previous unquantified LTC4412. PV-03 (reverse/cross-current under simultaneous USB+solar) remains `PHYSICAL_VALIDATION_REQUIRED`.
- **P01-R02 (DEC-03) — architecture conflict found, REOPENED, not resolved.** The package quotes MCP73871's own datasheet directly ("all limits apply for VIN = VREG + 0.3V to 6V"; absolute-max VIN = 7.0V) against the batch-6 panel target's V_OC≈7.2V (STC) / ≈8.0V (cold, -10°C, calculated from a generic silicon Voc temperature coefficient of -0.30…-0.35%/°C — itself `ASSUMED`, not the real panel's own datasheet). **This is independently verifiable from this register's own already-recorded data**: DEC-03's own primary-source citation (batch 1) already states MCP73871's abs-max VIN = 7V, and the batch-6 Design-input table already states the panel target's V_OC≈7.2V — 7.2 > 7.0 is a real, self-detected conflict, not merely the source document's claim. **Per explicit user instruction, this is flagged and not resolved here.** Two paths are on the table, neither selected: (a) replace MCP73871 with BQ24074 (28V input rating, 10.5V OVP, DPPM power-path — smallest architecture change, keeps power-path topology) or CN3791 (true switching MPPT, larger change); (b) keep MCP73871 and make solar-side OVP (already a candidate under P01-R06 below) a mandatory *precondition* rather than a parallel, independent decision, clamping the panel node below MCP73871's 7.0V abs-max before it ever reaches VIN. DEC-03 reopens from `DECIDED (candidate)` to `BLOCKED` — this is now GATE-2's most consequential open item, since it is a proven contradiction, not an unfilled selection. **Cascading flag:** `I_charger_quiescent` (30µA typ, from MCP73871's own IDISCHARGE figure, GAP-08) is provisional/at-risk pending this resolution — not changed here, since no replacement has been selected, but any future charger swap would need to re-source this term from the new part's own datasheet.
- **P01-R05 (DEC-12) promoted to `DECIDED (candidate)`** — candidate (b) (gate the I2C pull-up rail on the same switched rail as the sensor), implemented with TI TPS22916 (10nA leakage, integrated output discharge (QOD), reverse blocking), directly answering DEC-12's own three-way candidate list from batch 3. PV-04 (I2C back-powering / off-state leakage measurement) remains `PHYSICAL_VALIDATION_REQUIRED`. TPS22916's 10nA leakage is added to the sleep-current budget as a new, well-sourced term (see `power_budget_calc.py`).
- **P01-R06 (DEC-11) promoted to `DECIDED (candidate)`** — separate parts for solar and USB, resolving DEC-11's original blocking criterion (no IQ/leakage figure existed for any candidate): solar = Littelfuse SMAJ9.0A TVS (9.0V standoff, ~15.4V clamp, ~10µA max leakage — chosen over SMAJ8.0A, whose 8.0V standoff is judged too close to the calculated ~8.0V cold Voc); USB = Littelfuse SMF5.0A (simple TVS, 400µA max leakage, but only while USB is actually present — does not tax the sleep budget) or TI TPD1S514 (active OVP, if protection against bad chargers is also wanted). PV-05 (total daytime TVS leakage contribution to the sleep budget) remains `PHYSICAL_VALIDATION_REQUIRED`. The cold-Voc calculation (~8.0V) uses a **generic** silicon temperature coefficient, not the real (still unselected) panel's own datasheet — flagged `ASSUMED`, to be revisited once a real panel is sourced.
- **P01-R03 (DEC-04) advanced to `PROVISIONAL`** — Richtek RT9080 LDO (2µA Iq, 0.31V dropout @600mA) as the working default, chosen over AP2112K (55µA Iq, rejected) and XC6220 (8µA Iq) for lowest sleep-budget impact. **Explicit, bounded fallback condition**, not an open-ended maybe: if PV-01 (bench test — output <3.0V at 500mA RF-calibration peak, battery in the 3.3–3.7V range) shows brownout, switch to TI TPS63802 buck-boost (11µA Iq, regulates down to 2.75V cutoff, recovers the low-voltage tail) — this would add ~11µA to the sleep budget (≈+27%, per the returned package's own estimate). RT9080's part-level Iq is `SOURCE_SUPPORTED`; the LDO-vs-buck-boost choice itself remains `PROVISIONAL` pending PV-01, not `DECIDED`.
- **P01-R04 (DEC-13) advanced to `PROVISIONAL`** — factory-protected 18650 (e.g. a Samsung INR18650-30Q-class cell: 3000mAh nominal/2900mAh min, 3.6V nominal, 4.20V charge end, 2.5V discharge cutoff, 15A max continuous discharge, per Samsung SDI's own spec) as the working default, with DW01A+FS8205A board-level protection as the named alternative (VODP=2.40V min2.30/max2.50, VODR=3.00V recovery, VOCP=4.30V±50mV, ICC=3.0µA@3.6V, per its own datasheet). **Firmware threshold recommendation updated**: ~3.0–3.1V/cell critical-energy entry point (above DW01A's 2.40–2.50V cutoff range, leaving real margin) — this replaces the batch-4 `ASSUMED` ~3.4V placeholder with a `CALCULATED` value derived from named primary-sourced cutoff data, though still `PHYSICAL_VALIDATION_REQUIRED` for final cutoff-firmware coordination (PV-06). Remaining open: procurement of the specific cell (30Q or an equivalent ≥3000mAh protected part).

**GATE-2 net effect (not to be understated):** this batch closes more research packages at the "DECIDED (candidate), PV pending" level than any prior batch (P01-R01, P01-R05, P01-R06) and advances two more to `PROVISIONAL` with bounded fallback conditions (P01-R03, P01-R04) — but it also **reopens P01-R02/DEC-03**, previously the one item batch 5 had left untouched as `DECIDED (candidate)`, on a genuine, self-verifiable architecture conflict. GATE-2 remains BLOCKED; see the updated evaluation below.

## Design-input assumptions and target envelopes (2026-09-18, batch 4; updated batch 6)

Recorded per the user's explicit instruction to proceed on DEC-01/02/04/13 without a selected solar panel or battery cell (batch 4), then updated with `SOURCE_SUPPORTED (research-relayed)` target-class values from the batch-6 external research handoff. **None of these rows is a purchased part** — every physical-validation item they name (PV-01..05 and equivalents) remains open.

| Parameter | Value | Tier | Rationale |
|---|---|---|---|
| Solar panel V_MP | ≈6.0 V | SOURCE_SUPPORTED (research-relayed, batch 6) | 6 V-class/2 W-nominal target, sample panel datasheet class. Supersedes batch-4 `ASSUMED` 5.0 V. |
| Solar panel V_OC | ≈7.2 V | SOURCE_SUPPORTED (research-relayed, batch 6) | Same panel class. **Constrains DEC-01/DEC-11**: any arbitration/OVP part eventually selected must tolerate this open-circuit voltage, not just V_MP. Supersedes batch-4 `ASSUMED` 6.0 V. |
| Solar panel I_MP | ≥0.33 A | SOURCE_SUPPORTED (research-relayed, batch 6) | Supersedes batch-4 `ASSUMED` 150 mA. |
| Solar panel I_SC | ≈0.35 A | SOURCE_SUPPORTED (research-relayed, batch 6) | New row, not present in batch 4. |
| Solar panel P_max | ≥2 W nominal | SOURCE_SUPPORTED (research-relayed, batch 6) | Supersedes batch-4 `ASSUMED` ≈0.75 W; batch-6 rationale is a ≈67% STC current margin over a 1.2 W-class panel. |
| Battery chemistry/form | Protected 1S Li-ion **18650** | SOURCE_SUPPORTED (research-relayed, batch 6) | Supersedes batch-4's unspecified "1S Li-Ion/Li-Po" form factor; DEC-13 remains `RESEARCH_REQUIRED` for the specific model. |
| Battery capacity | ≥3000 mAh nominal (2000–3500 mAh research envelope) | SOURCE_SUPPORTED (research-relayed, batch 6) | Supersedes batch-4 `ASSUMED` 1000 mAh. |
| Battery nominal energy | 10.8 Wh (at 3000 mAh, 3.6 V class) | CALCULATED (batch 6) | Derived (capacity × nominal cell voltage). |
| Battery discharge-end reference | ≈2.75 V/cell (sample-datasheet value) | SOURCE_SUPPORTED (research-relayed, batch 6) | Not the actual protection-PCB cutoff — that is a separate, still-open value per the selected protection PCB's own datasheet. |
| Battery charge current limit | ≤0.5C (≤500 mA) | ASSUMED (batch 4, unchanged) | Conservative default; batch 6 confirms the panel's 0.33 A I_MP stays well under this, but P01-R02's actual charge-current tuning against the panel is `RESEARCH_REQUIRED`/`CALCULATED` per the returned package, not yet performed. |
| Battery operating window (firmware) | Full 4.2 V down to a firmware critical-energy threshold of **~3.0–3.1 V/cell** (updated batch 7, was ~3.4 V) | CALCULATED (batch 7, was ASSUMED batch 4) | **Batch 7 update:** derived from named primary-sourced cutoff data (DW01A VODP=2.40V min/2.50V max, VODR=3.00V recovery; Samsung 30Q-class discharge cutoff 2.5V) rather than an unattributed guess — leaves real margin above either protection path's cutoff. Still `PHYSICAL_VALIDATION_REQUIRED` for final cutoff-firmware coordination (PV-06); the exact protection-PCB model is still `RESEARCH_REQUIRED` (procurement). |
| Duty cycle (baseline policy) | 96 telemetry events/day (measurement every 5 min, Wi-Fi telemetry every 15 min), 15 s active/event, 180 mA active current | ASSUMED (batch 6) | New rows, not present in batch 4; `t_sleep`/`t_active`/`I_active` in `power_budget.md` §1 remain formally `OPEN` — these are example inputs for the batch-6 what-if calculation, not a frozen firmware duty cycle. |

**These values are still target envelopes and baseline policy assumptions, not purchased parts or a frozen duty cycle. They must be replaced with real selections/measurements (or explicitly re-confirmed as final) before any claim of GATE-2 numeric closure beyond "provisional."**

| ID | Question | Candidate options | Linked SOL-ID(s) | Status | Decision | Rationale |
|---|---|---|---|---|---|---|
| DEC-01 | Solar input topology | (a) reverse-blocking protection then charger input; (b) charger's dedicated solar/VIN pin with internal path management (rejected, false premise); (c) external priority-mux ideal-diode-OR ahead of MCP73871's single IN pin, using TI TPS2121; (d) Schottky OR-ing (SS34, simpler but ~0.4–0.5V charger-input-margin penalty) | SOL-001 | **DECIDED (candidate), 2026-09-19 batch 7 — tracked as P01-R01 (was `RESEARCH_REQUIRED` since batch 5)** | (c) — TI TPS2121, 2.8–22V input range per its own datasheet (SLVSDR8) | **Batch 4/5 history:** the original LTC4412+P-MOSFET candidate was promoted then correctly reverted (batch 5) for lacking a real comparison. **Batch 7 closure:** the returned package supplies what batch 5 required — a genuine 2–3-candidate comparison with primary datasheets and rejected alternatives. LM66100 (abs ±6V), LM66200 (abs 6V, 5.5V recommended) and TPS2113A-class (5.5V max) are all rejected on the same evidenced basis: none can tolerate the batch-6 panel target's raw Voc (~7.2V STC, ~8.0V cold) — they remain viable only on the charger-downstream system/battery rail (~3–4.2V), not the raw solar node this decision covers. TPS2121 (candidate c) is a single-chip priority mux confirmed by its own datasheet to span 2.8–22V, easily covering both 5V USB and the ~8V cold-Voc panel, with seamless reverse/cross-current blocking. Schottky OR-ing (candidate d) is kept as a simpler fallback if TPS2121's QFN package proves hand-assembly-prohibitive, at the cost of ~0.4–0.5V charger-input-margin loss. **Minor open item:** TPS2121's datasheet (2.8V) and product page (2.7V) disagree slightly on the lower bound — immaterial here since the upper 22V bound is what this design needs. **Not fully closed:** TPS2121's Iq is not quantified in the returned package (only "düşük-Iq" qualitatively) — `I_arbitration_Iq` in `05_power/power_budget.md` remains `ASSUMED`/`OPEN`, now attributed to TPS2121 rather than LTC4412. **Remaining:** PV-03 (reverse/cross-current under simultaneous USB+solar) `PHYSICAL_VALIDATION_REQUIRED`. |
| DEC-02 | USB-vs-solar source arbitration | (a) charger IC's own PowerPath/priority logic between two simultaneous sources (rejected, false premise); (b) external priority-mux ahead of MCP73871's single IN pin, using TI TPS2121 | SOL-002 | **DECIDED (candidate), 2026-09-19 batch 7 — tracked as P01-R01 (was `RESEARCH_REQUIRED` since batch 5)** | (b) — same TPS2121 stage as DEC-01 | See DEC-01 rationale; both are satisfied by the same single arbitration stage ahead of `CHARGER_IN`, so both are closed together at the same evidence tier. **Batch 6 finding retained:** the returned package's mandatory scenario list (USB-only/solar-only/both/source-switchover) matches `02_requirements/use_case_scenarios.md` P01-UC06 exactly. **Batch 7:** TPS2121 explicitly handles the "eşzamanlı USB+solar" (simultaneous USB+solar) case by selecting the higher-voltage source and blocking reverse current into the other — this is the source-state behavior P01-UC06 requires; PV-03 still validates it physically. |
| DEC-03 | Single-cell Li-Ion charger IC selection | Not yet candidate-listed | SOL-003 | **BLOCKED (reopened 2026-09-19 batch 7 — architecture conflict, tracked as P01-R02; was `DECIDED (candidate)` since batch 1)** | No selection — two paths identified, neither chosen (see rationale); MCP73871 is no longer a safe default | **Primary-source (DS22090, 2026-09-17):** recommended VIN = VREG+0.3 V to 6 V, absolute-max VIN = 7 V; supply current typ 260 µA (charge-complete), 180 µA (standby), 28 µA (shutdown); UVLO start typ = VREG+0.15 V, stop typ = VREG+0.07 V; hot-pluggable inputs require input overvoltage protection (→ DEC-11, OPEN). Single-cell Li-Ion/Li-Po charging, 4.2 V preset confirmed on its one IN pin. **GAP-03 CLOSED (batch 3, outcome B):** no source shows MCP73871's "overvoltage protection"/"thermal shutdown" as independent of its own charge-regulation loop — see DEC-13. **GAP-08 CLOSED (batch 3):** the applicable no-input term is IDISCHARGE @ VBAT=Power Out, No Load = 30 µA typ / 40 µA max. **Batch 6 update (P01-R02):** flagged MCP73871's VIN range sitting close to the new panel target's V_OC≈7.2V as needing real-panel measurement, but did not yet identify this as a hard conflict. **Batch 7 — REOPENED, architecture conflict found and self-verified:** the returned package quotes MCP73871's own datasheet directly — "all limits apply for VIN = VREG + 0.3V to 6V," absolute-max VIN = 7.0V — against this register's own batch-6 panel target of V_OC≈7.2V (STC) / ≈8.0V (cold, -10°C). **This conflict is independently checkable from data this register already held**: DEC-03's own batch-1 citation already recorded abs-max VIN=7V; batch 6 already recorded the panel's V_OC≈7.2V. 7.2V exceeds 7.0V — MCP73871 cannot be safely connected directly to this panel's raw output without protection, full stop. UVLO protects the low end; there is no equivalent internal high-side OVP. **Two paths, neither selected (per explicit user instruction to flag only):** (a) replace MCP73871 with BQ24074 (TI, 28V input rating, 10.5V OVP, DPPM-based power-path, VQFN16 — closest architectural match, same power-path pattern, safely covers the cold-Voc case) or CN3791 (Consonance, true switching-buck MPPT — larger change, higher energy harvest); (b) keep MCP73871 and make solar-side OVP (P01-R06's SMAJ9.0A or an active clamp) a **mandatory precondition** ahead of the charger's VIN pin, not an independently-scheduled decision. **Cascading flag:** `I_charger_quiescent` (30 µA typ, sourced from MCP73871's own IDISCHARGE figure) is provisional/at-risk pending this resolution — the number is not changed here since no replacement is selected, but a future charger swap would require re-sourcing this term from the new part's own datasheet. **Charge-current sub-item (batch 6) remains open regardless of path:** ISET tuning against the panel's 0.33A I_MP is still `CALCULATED`/`RESEARCH_REQUIRED`. |
| DEC-04 | 3.3 V regulator topology | (a) single low-Iq LDO/buck as the *only* 3.3 V rail (rejected — TPS7A02 specifically, see below); (b) dual-regulator split (rejected as architecturally pointless for this module); (c) single regulator with ≥0.5 A capability, replacing TPS7A02 entirely — part candidate: Richtek RT9080 LDO (PROVISIONAL); fallback: TI TPS63802 buck-boost | SOL-004 | **DECIDED (topology); part PROVISIONAL, 2026-09-19 batch 7 (tracked as P01-R03; part was `RESEARCH_REQUIRED` since batch 4)** | (c) — single ≥500 mA regulator; **PROVISIONAL part: Richtek RT9080** (2 µA Iq, 0.31 V dropout @600 mA), pending PV-01 | ESP32-WROOM-32E exposes one external 3.3 V supply pin, so a dual-regulator split (candidate b) would need its own output-arbitration stage — added complexity with no clear benefit. **TPS7A02 is dropped** (200 mA ceiling insufficient). **Batch 6:** reaffirmed the RF-calibration peak can exceed 500 mA; no part selected. **Batch 7 — PROVISIONAL part named, with an explicit bounded fallback condition:** RT9080 (Richtek, DS9080) chosen over AP2112K (55 µA Iq, rejected — too costly for the sleep budget) and XC6220 (8 µA Iq) for lowest Iq impact, per its own datasheet (2 µA Iq, 0.31 V dropout @600 mA, meaning VIN≥3.61V required at peak load). **Trigger condition, not an open-ended maybe:** if PV-01 (bench test: output <3.0V at 500 mA RF-calibration peak, battery in the 3.3–3.7V range) shows brownout, switch to TPS63802 buck-boost (TI, 11 µA Iq, regulates down to 2.75V cutoff) — this recovers the low-voltage tail at a cost of ≈+11 µA to the sleep budget (≈+27% per the returned package's own estimate, i.e. ≈40→≈51 µA once the budget is otherwise closed). RT9080's own Iq/dropout figures are `SOURCE_SUPPORTED`; the LDO-vs-buck-boost choice remains `PROVISIONAL`, not `DECIDED`, until PV-01 runs. The sleep-current budget's `I_regulator_Iq` term (`05_power/power_budget.md`) stays formally `OPEN` — a PROVISIONAL part is not yet evidence for closing that term, per this project's evidence-maturity axis. |
| DEC-05 | Battery-voltage divider gating | (a) GPIO-gated divider (removes leakage during sleep); (b) always-on divider (simpler, leakage accepted in budget) | SOL-005, SOL-015 | DECIDED (candidate) | (a) GPIO-gated | Removes an otherwise-continuous leakage term from the sleep-current budget for one extra GPIO and a firmware step; consistent with a sleep-dominant design goal. |
| DEC-06 | I2C sensor selection | Not yet candidate-listed | SOL-006, SOL-009, SOL-010, SOL-011, SOL-013 | DECIDED (candidate) | Bosch BME280 | **Primary-source (BST-DS002, 2026-09-17):** VDD = 1.71–3.6 V, VDDIO = 1.2–3.6 V (both span 3.3 V, confirms SOL-009/SOL-006's single 3.3 V-only domain); startup to first communication = 2 ms (closes the interface contract §6 stabilization-delay magnitude, GAP-05); sleep current = 0.1 µA typ / 0.3 µA max (typ/max distinction preserved); address confirmed SDO=GND→0x76, SDO=VDDIO→0x77. GAP-05 closed. |
| DEC-07 | Deep-sleep wake-trigger source | (a) RTC timer only; (b) RTC timer + GPIO/external interrupt | SOL-007 | DECIDED (candidate) — **mode corrected** | (a) RTC timer only, using ESP32 **Deep-sleep with RTC timer + RTC memory retained (10 µA)** — **not** Hibernation with RTC-timer-only (5 µA) | No event source needing asynchronous wake is in scope, so RTC-only wake stands. **Primary-source (ESP32 Series Datasheet v5.3, 2026-09-17):** Deep-sleep w/ RTC timer + RTC memory = 10 µA; Hibernation w/ RTC timer only = 5 µA; power-off = 1 µA. Hibernation is *not* compatible with this design: DEC-09's cross-cycle consecutive-fault counter must persist across sleep to count N consecutive faults, and Hibernation discards RTC memory (the only memory that survives ESP32 deep sleep), which would silently reset the counter every cycle and defeat DEC-09. The correct candidate is therefore the 10 µA mode, not the lower 5 µA figure — selecting 5 µA here would be an unsupported simplification that contradicts an already-decided requirement (DEC-09). Supersedes the earlier `<5 µA` bare-chip search-corroborated figure. |
| DEC-08 | Duplicate-address handling clause | Candidate: `NOT_APPLICABLE` (single fixed-address sensor); only revisited if a second same-address device is added | SOL-012 | DECIDED (candidate) | `NOT_APPLICABLE` | DEC-06 selects a single BME280; its address is strap-selectable but only one device is on the bus, so no duplicate-address condition exists. |
| DEC-09 | Consecutive-fault escalation behavior | (a) no escalation, every cycle independent; (b) extend the next sleep interval after repeated faults; (c) after N consecutive faults, escalate the telemetry status from per-cycle `FAULT` to a persistent `SENSOR_OFFLINE` status, with sleep timing unchanged | SOL-011, SOL-014 | DECIDED (candidate) | (c) — persistent `SENSOR_OFFLINE` telemetry status after N consecutive faults; sleep-cycle timing (§SOL-007/DEC-07) is not altered by fault state | (a) leaves a permanently failed sensor indistinguishable from a one-off bus glitch in the telemetry stream, which defeats the purpose of a fault status. (b) couples a firmware reliability concern to the power/duty-cycle design, adding a second numeric axis (extension factor) to an already-open sleep-interval decision, and risks masking a hardware fault as reduced update rate. (c) adds one soft firmware counter (N, an implementation constant — not a hardware/power assumption) without touching the power-state model's timing, keeping the fault-escalation decision independent of and non-blocking for the sleep-current budget (P01-06). N's specific value remains an implementation-time choice (candidate N=3), not a requirement. |
| DEC-10 | PCB stackup / analog-digital separation specifics | Deferred — implementation-stage decision, not evaluated before GATE-2 | SOL-008 | OPEN (deferred) | — | Out of scope until after GATE-2. |
| DEC-11 | External input overvoltage protection for hot-pluggable solar/USB inputs | See functional spec and candidates below | SOL-001, SOL-002 | **DECIDED (candidate), 2026-09-19 batch 7 — tracked as P01-R06 (was `BLOCKED` since batch 3)** | Solar: Littelfuse **SMAJ9.0A** TVS; USB: Littelfuse **SMF5.0A** TVS (simple) or TI **TPD1S514** (active OVP, if bad-charger protection is also wanted) | Surfaced by DEC-03: hot-pluggable inputs need OV protection. **Batch 2/3 candidates (NCP346, NCP361, NUS6160MN, TPD4S014) — superseded, not carried forward:** none had a sourced IQ/leakage figure, the exact closure blocker; the batch-7 package instead proposes TVS-clamp candidates with sourced leakage. **Batch 6:** established solar and USB need *separate* assessments (different max voltages). **Batch 7 closure — the original blocking criterion is met:** solar candidate SMAJ9.0A (Littelfuse, 9.0V standoff, ~15.4V clamp, ~10µA max leakage per its own datasheet) is chosen over SMAJ8.0A (8.0V standoff — judged too close to the ~8.0V calculated cold-Voc, where guaranteed-clamp margin and leakage both degrade). USB candidate SMF5.0A (Littelfuse, 400µA max leakage, but only while USB is physically present — does not tax the sleep budget) is the simple option; TPD1S514 (TI, 30V DC tolerant, precision clamp, <100ns response) is the active-OVP option if protection against damaged/out-of-spec chargers is also wanted, at a small added LDO-mode current (3mA @5V, again only while USB is present). **Cold-Voc calculation (CALCULATED, batch 7):** Voc(-10°C) ≈ 7.2V × [1 + 0.0035×35] ≈ 8.08V, using a **generic** silicon Voc temperature coefficient (-0.30…-0.35%/°C, IEC 61215-class industry range) — `ASSUMED`, not the real (still unselected) panel's own datasheet; must be revisited once a real panel is sourced, per this decision's own recommendation. **Remaining:** PV-05 (total daytime TVS leakage contribution to the sleep budget) `PHYSICAL_VALIDATION_REQUIRED`. Interacts with, does not resolve, DEC-03's reopened MCP73871/Voc conflict — this OVP stage is one of the two paths DEC-03 lists, not an independent fix. |
| DEC-12 | BME280 I2C-line logic level during power-gating | (a) gate the sensor's power rail (VDD/VDDIO) only, leave I2C pull-ups on the always-on 3.3 V rail (rejected — forbidden condition); (b) gate the pull-ups on the same switched rail as the sensor, using TI TPS22916; (c) do not gate VDDIO (only gate VDD), keep VDDIO always-on | SOL-006, SOL-013 | **DECIDED (candidate), 2026-09-19 batch 7 — tracked as P01-R05 (was `OPEN` since batch 3)** | (b) — gate `SENSOR_3V3` (VDD+VDDIO together) via TI **TPS22916** load switch; I2C pull-ups on the same switched rail | **New finding, Drive-relayed, citing Bosch directly (batch 3):** BME280's datasheet forbids holding SDI/SDO/SCK/CSB at logic-high while VDDIO is off — candidate (a) (the original design, pull-ups on an always-on rail) is exactly this forbidden condition and is rejected. **Batch 6:** the returned package confirmed candidate (b)'s topology direction ("VDD+VDDIO switched together on SENSOR_3V3, pull-ups on the same switched rail, I2C GPIOs forced safe/high-Z before power-down") as `SUPPORTED`, without naming a switching component. **Batch 7 closure:** TPS22916 (TI) is named as the switching element — 10nA leakage, integrated output discharge (QOD) that actively pulls `SENSOR_3V3` down when switched off (further reducing the window where a forbidden VDDIO-off/pin-high state could occur), and reverse blocking. The returned package also flags a related risk this decision now explicitly covers: with pull-ups on `SENSOR_3V3` and ESP32 GPIOs forced high-Z before shutdown, TPS22916's QOD prevents I2C-pull-up back-powering of the sensor through its own ESD diodes while `SENSOR_3V3` is off. TPS22917 (SOT-23, easier hand-assembly) is the named alternative if TPS22916's WCSP/SOT package proves hard to hand-solder. **Sleep-budget addition:** TPS22916's 10nA leakage is added as a new, well-sourced term (see `power_budget_calc.py`) — negligible (0.01 µA) next to the existing µA-scale terms. **Remaining:** PV-04 (I2C back-powering / off-state leakage measurement) `PHYSICAL_VALIDATION_REQUIRED` before `04_interface/i2c_interface_contract.md` §6 can be marked fully closed. |
| DEC-13 | Independent battery-side protection (separate from MCP73871's charge-regulation loop) | (a) require a **protected** 1S Li-Ion/Li-Po cell (protection IC integrated in the pack) — e.g. Samsung INR18650-30Q-class; (b) design a standalone protection circuit around an unprotected cell, using DW01A+FS8205A | SOL-003 | **PROVISIONAL, 2026-09-19 batch 7 — tracked as P01-R04 (was `RESEARCH_REQUIRED` since batch 5)** | (a), **PROVISIONAL**: factory-protected 18650, e.g. Samsung INR18650-30Q-class or equivalent ≥3000 mAh protected cell; (b) DW01A+FS8205A named as the concrete alternative if (a) proves unsourceable | GAP-03 (outcome B) established MCP73871 does not itself provide independent battery-terminal protection. **Batch 4/5/6 history:** candidate (a) was the working-lean direction throughout, but downgraded in batch 5 for lacking cutoff/leakage evidence; batch 6 narrowed the target to a protected 18650 class without a real model. **Batch 7 advance to PROVISIONAL:** the returned package names concrete, primary-sourced options for both candidates — (a) Samsung INR18650-30Q (Samsung SDI spec: 3000mAh nominal/2900mAh min, 3.6V nominal, 4.20V charge end, 2.5V discharge cutoff, 15A max continuous discharge, ≤26mΩ AC impedance @1kHz) as a concrete example of the target class; (b) DW01A+FS8205A (VODP=2.40V min2.30/max2.50, VODR=3.00V recovery, VOCP=4.30V±50mV, ICC=3.0µA@3.6V, per its own datasheet) as board-level protection, whose cutoff (2.40–2.50V) sits *below* the batch-6 target's 2.75V discharge-end reference — meaning (b) alone discharges the cell deeper than (a)'s target, so the firmware critical-energy threshold (now ~3.0–3.1V/cell, see "Design-input assumptions" above) must sit comfortably above whichever protection path is used. (a) remains the lower-risk default (protection delegated to a qualified pack assembly); (b) is the named fallback, not rejected. **Not `DECIDED`:** the specific protected-cell model (30Q vs. 35E vs. an equivalent) still needs procurement verification, and PV-06 (cutoff-firmware coordination) is `PHYSICAL_VALIDATION_REQUIRED` regardless of which path is used. |

All "DECIDED (candidate)" rows are engineering decisions this session is authorized to make for a context-bound portfolio reference project (no client is dictating parts). They are not `VERIFIED` claims about the parts themselves — component-level numeric values inherit the evidence-acquisition constraint above until a primary datasheet is directly read or a bench measurement exists.

## GATE-2 evaluation

Per the governance report §2.1: *"I2C interface contract + power-state model + source/decision-supported numeric assumptions hazır olmadan PCB implementation başlamaz."* Re-evaluated once under the 17.09.2026 REFERENCE_BASELINE (P01-G2-05), after all four named closure contracts (GAP-08, GAP-03, GAP-02, DEC-11) reached a documented closure state.

**Closure contract outcomes:**
1. **GAP-08 — CLOSED.** `I_charger_quiescent` = 30 µA typ (IDISCHARGE @ VBAT=Power Out, No Load). Sleep-current total now computes: **40.125 µA typ** (verified by running `power_budget_calc.py`).
2. **GAP-03 — CLOSED (outcome B).** MCP73871 does not provide independent battery-side protection; assumption rejected; DEC-13 opened.
3. **GAP-02 — CLOSED (analytical).** Pull-up sized against UM10204's own worst-case Cb=400 pF: R_min=967 Ω, R_max=2951 Ω. Physical rise-time confirmation queued (`PHYSICAL_VALIDATION_REQUIRED`).
4. **DEC-11 — still BLOCKED.** No quiescent-current evidence found for any remaining OVP candidate; re-affirmed BLOCKED rather than a speculative selection, per instruction.

**GATE-2 status: BLOCKED (unchanged since last evaluation — not yet due for re-evaluation).**

**Update (2026-09-18, batch 4):** per the external handoff document's own recommended order ("start with a compact decision package for DEC-01/DEC-02/DEC-04/DEC-13, then resolve BME280 gating and DEC-11 against the frozen topology"), three of those four are now closed at the topology/candidate level:

1. **DEC-01/DEC-02 — DECIDED (candidate).** External ideal-diode-OR (LTC4412 + P-FET) ahead of MCP73871's single IN pin. Source-state table, reverse-current-path diagram and bench no-backfeed verification still needed before schematic-ready.
2. **DEC-04 — DECIDED (topology only).** Single ≥500 mA regulator replaces TPS7A02 entirely; **exact part still OPEN** — no low-IQ ≥500 mA part has been verified, so the sleep-current budget's regulator term reverts to `OPEN` (see `05_power/power_budget.md`).
3. **DEC-13 — DECIDED (candidate).** Protected 1S cell mandated for Rev-A; exact cell/cutoff specs still OPEN (ASSUMED table above).

**Still blocking GATE-2, in the handoff's own stated order:**
- DEC-04's regulator **part selection** (topology decided, part not).
- **DEC-12** — BME280 I2C pin logic-level-during-power-gating hazard; not addressed this pass, deliberately deferred until the source/regulator topology above was frozen.
- **DEC-11** — input OVP component selection; still `BLOCKED`, no IQ evidence for the remaining candidates; also deliberately deferred until this pass's topology was frozen (an OVP stage sits electrically close to the new arbitration stage, so sequencing it after DEC-01/02 is intentional, not neglect).

**Update (2026-09-18, batch 5 — revised handoff):** DEC-01, DEC-02 and DEC-13, closed as `DECIDED (candidate)` above in batch 4, are **downgraded back to `RESEARCH_REQUIRED`** (see the "Revised closure model" section above) under the revision's stricter seed-candidate rule. GATE-2's blocking list is therefore **wider than the batch-4 report stated**, not narrower. Restated in the revision's own research dependency order (**P01-R01 → P01-R02 → P01-R03 → P01-R04 → P01-R05/P01-R06 → P01-R07 → P01-R08**, per its §21 "revised immediate next action"):
- **P01-R01** — DEC-01/DEC-02 (source arbitration) — reopened to `RESEARCH_REQUIRED`.
- P01-R02 — DEC-03 (charger/power-path) — `DECIDED (candidate)`, not blocking.
- **P01-R03** — DEC-04's regulator part selection — unchanged, still open.
- **P01-R04** — DEC-13 (battery-side protection) — reopened to `RESEARCH_REQUIRED`.
- **P01-R05/P01-R06** — DEC-12 and DEC-11 — unchanged, still OPEN/BLOCKED.
- P01-R07/P01-R08 — battery sensing/ADC and board-level energy model — new packages, not yet started (see `02_requirements/use_case_scenarios.md`, `05_power/calculation_packages.md`); not reachable before P01-R01..R06 close.

Per the revision's own operating principle, none of these are to be closed by this session directly — they are to be transferred to the separate research project, which returns compact closure packages (evidence, reproducible calculations, rejected alternatives, residual validation, proposed status) for this register to then accept. This session's role until that happens is limited to maintaining the scaffolding (`02_requirements/use_case_scenarios.md`, `05_power/calculation_packages.md`) the returned packages will need to plug into.

PCB/firmware implementation (P01-08) does not open. This session does not proceed to it. GATE-2 will not be re-evaluated until DEC-01/DEC-02 (P01-R01), DEC-04's part (P01-R03), DEC-13 (P01-R04), DEC-12 (P01-R05) and DEC-11 (P01-R06) each reach their own documented closure, in that dependency order, per the 17.09 baseline's "re-evaluate once, after all named contracts close" rule.

**Update (2026-09-18, batch 6 — Perplexity supplementary research handoff):** this package narrows the *numeric design-input envelope* for the solar panel and battery (see "Design-input assumptions" above) and provides a provisional daily-energy/autonomy example, but per its own explicit statement it closes no P01-Rxx package — no component model number is selected for P01-R01/R02(part)/R03/R04/R06, and P01-R05's confirmation is a direction check, not a closure. **GATE-2 status: unchanged, still BLOCKED**, on the same five items in the same dependency order (P01-R01, P01-R03, P01-R04, P01-R05, P01-R06). This is expected and correct, not a shortfall: a numeric-envelope narrowing is real progress toward eventual closure but is explicitly distinct from a component selection or a physical validation, per this project's own evidence-maturity axis.

**Update (2026-09-19, batch 7 — GATE-2 component-selection research handoff):** the most substantial single-batch movement in this register's history, in both directions:

- **Closed at `DECIDED (candidate)` level (comparison + rejected alternatives + PV items queued):** P01-R01 (TPS2121), P01-R05 (TPS22916), P01-R06 (SMAJ9.0A + SMF5.0A/TPD1S514).
- **Advanced to `PROVISIONAL` (named part + explicit bounded fallback condition):** P01-R03 (RT9080, fallback TPS63802 on PV-01 failure), P01-R04 (factory-protected 18650, fallback DW01A+FS8205A).
- **Reopened to `BLOCKED` (self-verified architecture conflict, not a mere gap):** P01-R02/DEC-03 — MCP73871's own abs-max VIN (7.0V, recorded since batch 1) is exceeded by this register's own batch-6 panel target V_OC (≈7.2V STC, ≈8.0V cold). Two replacement/mitigation paths are named; **neither is selected, per explicit instruction to flag only.**

**GATE-2 status: still BLOCKED — the blocking set has changed shape, not shrunk to zero.** Three items (P01-R01, P01-R05, P01-R06) are now at their most-closed practical state short of physical validation. Two items (P01-R03, P01-R04) are PROVISIONAL with named, testable exit conditions rather than fully open. But **P01-R02/DEC-03 is now the sharpest blocker in this register's history** — not an unfilled selection, but a proven contradiction between two facts this project itself already recorded. GATE-2 cannot close, and should not be re-evaluated as "mostly done," until DEC-03 is resolved one way or the other; the other five items' progress does not offset this. Per the revised handoff's operating principle, DEC-03's resolution is an architecture decision, not a research-package output — it is not this session's to make unprompted.
